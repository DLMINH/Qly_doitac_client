(function() {
    var app = angular.module("contract", []);
    app.controller('contractCtrl', ['$scope', 'contractService', '$location', '$rootScope', '$window',
        function($scope, contractService, $location, $rootScope, $window) {
            
        }
    ])
}());
